export function ToolingPreview() {
  return (
    <main className="preview-shell">
      <h1>Norman World</h1>
      <p>
        React, TypeScript, Vite, Vitest, and Bun are installed for deterministic
        tooling. The production site remains the root static HTML published by
        GitHub Pages.
      </p>
    </main>
  );
}
